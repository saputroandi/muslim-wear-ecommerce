<!doctype html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Admin</title>
    <link rel="stylesheet" href="/css/design.css" />
  </head>
  <body>
    <main class="section container">
      <h1 class="section-title">Admin Login</h1>
      {{#if error}}
        <div class="card" style="border-left:4px solid #f87171">{{error}}</div>
      {{/if}}
      <form method="post" action="/auth/login" style="max-width:420px">
        <input type="hidden" name="_csrf" value="{{csrfToken}}" />
        <label for="email">Email</label>
        <input id="email" name="email" type="email" required style="width:100%;padding:8px;margin:8px 0;" />
        <label for="password">Password</label>
        <input id="password" name="password" type="password" required style="width:100%;padding:8px;margin:8px 0;" />
        <div style="margin-top:12px">
          <button class="pill-button primary" type="submit">Login</button>
        </div>
      </form>
    </main>
  </body>
</html>
